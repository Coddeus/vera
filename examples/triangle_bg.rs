use vera::Vera;

fn main() {
    let v = Vera::create(800, 600);
    v.triangle();
    v.show();
    
    // instance.show()
    // instance.save_frame(14.5);
    // instance.save();
    // instance.show_and_save()
}